apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "books-app.fullname" . }}-test-connection"
  labels:
    {{- include "books-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "books-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
