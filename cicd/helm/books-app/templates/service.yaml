apiVersion: v1
kind: Service
metadata:
  name: {{ include "books-app.fullname" . }}
  labels:
    {{- include "books-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "books-app.selectorLabels" . | nindent 4 }}
